import { LoginForm } from '../../components/forms/login/loginform';
import styles from './login.module.css';

export const Login = () => {
  const test = '';

  return (
    <section>
      <LoginForm />
    </section>
  );
};
